import React from 'react';

const filterButton = (name, min, max) => {
  return (
    <button className="filter-metric-button">

    <button>

  )
};

const Filters = ({ children }) => (
  <div>

  </div>
);

export default Filters;
